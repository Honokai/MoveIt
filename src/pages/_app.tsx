import '../estilizacao/global.css';
import { DesafiosProvider } from '../contextos/ContextosDesafios';

function MyApp({ Component, pageProps }) {
  return (
    <Component {...pageProps} />
  )
}

export default MyApp
